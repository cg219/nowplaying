lastfm:
  key: lastfm api key
  secret: lastfm api secret
turso:
  name: database name
  token: database token
  url: database url
spotify:
  id: client id
  secret: client secret
  redirect: redirect uri
app:
  name: app username
  id: app user id
twitter:
  id: client id
  secret: client secret
  redirect: redirect uri
r2:
  key: r2 key
  secret: r2 secret
  url: r2 url
  token: r2 token

